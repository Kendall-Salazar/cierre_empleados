[phases.setup]
nixPkgs = ["...", "nodejs_20", "nodePackages.npm"]

[phases.build]
dependsOn = ["install"]
cmds = ["cd frontend && npm install --include=dev --registry=https://registry.npmjs.org/ --no-audit --no-fund && npm run build"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
